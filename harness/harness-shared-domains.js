// harness-shared-domains.js — OBSOLETE, safe to delete.
//
// Tested box-code/shared-domains.js, the whole-domain block guardrail. Whole-domain blocking was
// removed entirely on 2026-07-27 (only single addresses can be blocked now), so both the guardrail
// and this harness have nothing left to cover. See routes/admin.js POST /item/:id/block.
console.log("harness-shared-domains: obsolete (whole-domain blocking was removed) — nothing to test.");
process.exit(0);
